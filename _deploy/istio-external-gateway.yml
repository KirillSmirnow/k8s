apiVersion: networking.istio.io/v1
kind: Gateway
metadata:
  name: istio-external-gateway
spec:
  selector:
    istio: ingressgateway
  servers:
    - hosts:
        - '*'
      port:
        name: http
        number: 80
        protocol: http
